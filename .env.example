# Provider — pick one. Falls back to FakeListChatModel if both empty.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Override default model per provider
OPENAI_MODEL=gpt-4o-mini
ANTHROPIC_MODEL=claude-3-5-haiku-latest
